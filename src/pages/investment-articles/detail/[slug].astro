---
import { decodeSlug } from "emdash";

const slug = decodeSlug(Astro.params.slug);
if (!slug) {
	return Astro.redirect("/404");
}

return Astro.redirect(`/investment-articles/${encodeURIComponent(slug)}`, 301);
---
